# FinRAG Environment Variables
# Copy this file to .env and fill in real values.
# NEVER commit .env — it is listed in .gitignore.

# ── LLM ──────────────────────────────────────────────────────────────────────
# OpenAI API key for GPT-4o-mini generation
OPENAI_API_KEY=sk-replace-me

# OpenAI model to use (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# ── Embedding ─────────────────────────────────────────────────────────────────
# HuggingFace model ID for sentence-transformer embeddings
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# Local cache directory for downloaded model weights
MODEL_CACHE_DIR=./models

# ── Vector Store ─────────────────────────────────────────────────────────────
# Persistent directory for ChromaDB data
CHROMA_PERSIST_DIR=./chroma_db

# ChromaDB collection name
CHROMA_COLLECTION=finrag_docs

# Optional: ChromaDB auth token (leave blank for local dev)
CHROMA_AUTH_TOKEN=

# ── Retrieval ─────────────────────────────────────────────────────────────────
# Default number of chunks to retrieve
TOP_K=5

# MMR lambda — 1.0 = pure relevance, 0.0 = pure diversity
MMR_LAMBDA=0.7

# ── Chunking ──────────────────────────────────────────────────────────────────
CHUNK_SIZE=512
CHUNK_OVERLAP=64

# ── File Ingestion ────────────────────────────────────────────────────────────
# Maximum allowed upload size in bytes (default 50 MB)
MAX_FILE_SIZE_BYTES=52428800

# ── API ───────────────────────────────────────────────────────────────────────
# Host/port for the FastAPI server
API_HOST=0.0.0.0
API_PORT=8000

# CORS allowed origins (comma-separated; restrict in production)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Requests per minute limits for rate limiting
RATE_LIMIT_INGEST=10
RATE_LIMIT_QUERY=30

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO
